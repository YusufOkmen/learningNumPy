NumPy is a Python library used for working with arrays.

It also has functions for wokring domain of linear algebra, fourier transform, and matrices.

In Python we have lists that serve the purpose of arrays, but they are slow to porcess. NumPy aims to provide an array object that is up to 50x faster than traditional Python lists.